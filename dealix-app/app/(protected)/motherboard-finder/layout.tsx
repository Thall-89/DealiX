import type { Metadata } from "next";
export const metadata: Metadata = { title: "Motherboard Finder" };
export default function MotherboardFinderLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
